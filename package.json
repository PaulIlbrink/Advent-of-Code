{
  "name": "advent-of-code",
  "type": "module",
  "module": "runner.ts",
  "scripts": {
    "start": "bun runner.ts",
    "run": "bun runner.ts",
    "watch": "bun --watch runner.ts",
    "benchmark": "bun runner.ts --benchmark",
    "benchmark:watch": "bun runner.ts --benchmark --watch",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "chalk": "^5.6.2"
  }
}
